<%= simple_form_for [:cashier, @order] do |form| %>
    <%= form.collection_check_boxes :dish_ids, Dish.all, :id, :name %>
    <%= form.select :customer_id, Customer.all.map { |r| [r.user.email, r.id ] }, include_blank: false %>

    <%= form.submit "Submit" %>
<% end %>


