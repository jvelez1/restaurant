<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<table>
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <thead>
  <tr>
    <th> Correo</th>
    <th>Status</th>
    <th>Total</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @orders.each do |order| %>
      <tr>
        <td><%=order.customer.user.email%> </td>
        <td><%=order.state%></td>
        <td><%=order.total%></td>
        <td><%= link_to 'Finish', finished_chef_orders_path(id: order), method: :put %></td>
      </tr>
  <% end %>
  </tbody>

</table>